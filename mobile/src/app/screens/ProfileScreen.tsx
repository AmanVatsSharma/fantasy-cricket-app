/**
 * File:        mobile/src/app/screens/ProfileScreen.tsx
 * Module:      Account · Profile Screen
 * Purpose:     Premium account page matching reference design exactly
 *
 * Exports:
 *   - ProfileScreen   — Account profile with stats, wallet actions, quick links, and settings
 *
 * Author:      Aman Vats Sharma
 * Last-updated: 2026-05-20
 */

import React from 'react';
import { AccountScreen } from './AccountScreen';

export const ProfileScreen = (props: any) => {
  return <AccountScreen {...props} />;
};
